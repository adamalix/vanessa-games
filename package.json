{
  "name": "vanessa-games",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "format": "pnpm exec oxfmt --write .",
    "format:check": "pnpm exec oxfmt --list-different .",
    "typecheck": "pnpm exec oxlint .",
    "lint": "pnpm exec oxlint .",
    "lint:fix": "pnpm exec oxlint . --fix",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "oxlint-tsgolint": "^0.20.0",
    "typescript": "^6.0.2"
  }
}
